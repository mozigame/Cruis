<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.magic.crius.dao.crius.db.OwnerReforwardMoneyToGameMapper">
    <resultMap id="BaseResultMap" type="com.magic.crius.po.OwnerReforwardMoneyToGame">
        <id column="Id" jdbcType="INTEGER" property="id"/>
        <result column="owner_id" jdbcType="BIGINT" property="ownerId"/>
        <result column="game_type" jdbcType="VARCHAR" property="gameType"/>
        <result column="order_num" jdbcType="INTEGER" property="orderNum"/>
        <result column="order_count" jdbcType="BIGINT" property="orderCount"/>
        <result column="effect_order_count" jdbcType="BIGINT" property="effectOrderCount"/>
        <result column="reforward_money_count" jdbcType="BIGINT" property="reforwardMoneyCount"/>
        <result column="pdate" jdbcType="INTEGER" property="pdate"/>
    </resultMap>
    <sql id="Base_Column_List">
    Id, owner_id, game_type, order_num, order_count, effect_order_count, reforward_money_count,
    pdate
  </sql>
    <select id="selectByPrimaryKey" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from owner_reforward_money_to_game
        where Id = #{id,jdbcType=INTEGER}
    </select>
    <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer">
    delete from owner_reforward_money_to_game
    where Id = #{id,jdbcType=INTEGER}
  </delete>

    <!--添加-->
    <insert id="insert" parameterType="com.magic.crius.po.OwnerReforwardMoneyToGame">
        insert into owner_reforward_money_to_game (owner_id, game_type,
          order_num, order_count, effect_order_count,
          reforward_money_count, pdate)
        values (#{ownerId,jdbcType=BIGINT}, #{gameType,jdbcType=VARCHAR},
          #{orderNum,jdbcType=INTEGER}, #{orderCount,jdbcType=BIGINT}, #{effectOrderCount,jdbcType=BIGINT},
          #{reforwardMoneyCount,jdbcType=BIGINT}, #{pdate,jdbcType=INTEGER})
    </insert>

    <!--批量添加-->
    <insert id="batchInsert" parameterType="com.magic.crius.po.OwnerReforwardMoneyToGame">
        insert into owner_reforward_money_to_game (owner_id, game_type,
        order_num, order_count, effect_order_count,
        reforward_money_count, pdate)
        values
        <foreach collection="list" item="param" separator="," >
          (
            #{param.ownerId,jdbcType=BIGINT},
            #{param.gameType,jdbcType=VARCHAR},
            #{param.orderNum,jdbcType=INTEGER},
            #{param.orderCount,jdbcType=BIGINT},
            #{param.effectOrderCount,jdbcType=BIGINT},
            #{param.reforwardMoneyCount,jdbcType=BIGINT},
            #{param.pdate,jdbcType=INTEGER})
        </foreach>

    </insert>

    <update id="updateSummary" parameterType="com.magic.crius.po.OwnerReforwardMoneyToGame">
        update owner_reforward_money_to_game
        set order_num =order_num + #{orderNum,jdbcType=INTEGER},
          order_count = order_count + #{orderCount,jdbcType=BIGINT},
          effect_order_count = effect_order_count + #{effectOrderCount,jdbcType=BIGINT},
          reforward_money_count = reforward_money_count + #{reforwardMoneyCount,jdbcType=BIGINT}
        where owner_id = #{ownerId,jdbcType=BIGINT}
          AND pdate = #{pdate,jdbcType=INTEGER}
          AND game_type = #{gameType,jdbcType=VARCHAR}
    </update>


    <!--查询多个业主下的数据-->
    <select id="findByOwnerIds" resultMap="BaseResultMap">
        SELECT owner_id,game_type FROM owner_reforward_money_to_game
        where pdate = #{pdate,jdbcType=INTEGER}
        AND owner_id IN
        <foreach collection="list" item="ownerId" separator="," open="(" close=")">
            #{ownerId,jdbcType=BIGINT}
        </foreach>

    </select>
</mapper>